[package]
name = "heft-core"
version = "1.0.0"
edition = "2021"
rust-version = "1.75"
description = "Parallel disk scanning, squarified treemaps and stats for Heft"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[profile.release]
lto = true
codegen-units = 1
